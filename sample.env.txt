# Cron schedule for feed checks (default: every 15 minutes). Example: */5 * * * * = every 5 min
# CRON_SCHEDULE=*/15 * * * *

# Log-only mode: set to 1, true, or yes to skip Telegram and only log messages
# LOG_ONLY=1

# Telegram Bot Configuration (not required when LOG_ONLY=1)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
TELEGRAM_CHAT_ID=your_telegram_chat_id_here

# Prowlarr Configuration
PROWLARR_URL=your_prowlarr_url_here
PROWLARR_API_KEY=your_prowlarr_api_key_here
INDEXER_ID=your_indexer_id_here

# Flare Resolver Configuration (for bypassing Cloudflare)
FLARE_RESOLVER_URL=your_flare_resolver_url_here

# Max retries for fetching file details (magnet/release type) per item (default: 3)
# MAX_RETRIES=3

# Docker Configuration (for building/pushing the image)
DOCKER_IMAGE_NAME=prowlarr-to-telegram
DOCKER_REGISTRY=your_docker_registry_here